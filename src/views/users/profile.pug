extends ../base
include ../mixins/video

block content 
    div.profile__data
        img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.videos-grid
        each ownVideo in user.videos
            +video(ownVideo)
        else
            span.empty__message No videos found